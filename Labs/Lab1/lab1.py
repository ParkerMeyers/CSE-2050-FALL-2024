def generic_hi(name="world"):
    """
    Generic hello world function, greets the name argument or by default greets the world
    Args:
        name (string): The name to greet, default "world"

    Returns:
        string: The combined welcoming string
    """
    return "Hello, " + name + "!"